syntax = "proto3";

package main;

option go_package = "./main";

message Request {
  string name = 1;
}

message Response {
  string type_url = 1;
  bytes value = 2;
}

service Entrypoint {
  rpc Greet (Request) returns (Response) {
  };
};
